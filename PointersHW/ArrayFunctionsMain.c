#include <stdio.h>
#include "" 

int main(){
    // int arr[]={3,7,3,0,5,19,3,28,19,25};
    // shift(arr, 2, 5);
    // print_array(arr, 10);
    // int arr2[]={2,35,72,13,10,48,7,3,14,21};
    // int arr3[]={0,0,0,1,1,0,1,0,0,1};
    // int* sortedArray = Bubble_Sort(arr, 10);
    // print_array(sortedArray, 10);
    // int Max_Occur = Max_Occurance_Value(arr, 10);
    // printf("the most occurance value is %d\n", Max_Occur);
    // int evens = even_then_odd(arr2, 10);
    // print_array(arr2, 10);
    // int arrangedZeroesAndOnes = Zeroes_then_Ones(arr3, 10);
    // print_array(arr3, 10);
    // srand(time(NULL));
    // int arr[60000];
    // int arr2[60000];
    // struct timespec start, end, start2, end2;
    // double elapsed, elapsed2;
    // int temp[60000];

    // random_arr(arr2, 60000);

    // for(int i=0; i<60000; i++){
    //     temp[i] = arr2[i];
    // }


    // clock_gettime(CLOCK_MONOTONIC, &start);
    // int* sortedArray = Bubble_Sort(arr2, 60000);
    // clock_gettime(CLOCK_MONOTONIC, &end); 

    // clock_gettime(CLOCK_MONOTONIC, &start2);
    // int* sortedArray2 = Bubble_Sort_reg(temp, 60000);
    // clock_gettime(CLOCK_MONOTONIC, &end2); 
  
    // elapsed = (end.tv_sec - start.tv_sec)
    //         + (end.tv_nsec - start.tv_nsec) / 1e9;

    // elapsed2 = (end2.tv_sec - start2.tv_sec)
    //         + (end2.tv_nsec - start2.tv_nsec) / 1e9;

    // printf("Time: %f seconds\n", elapsed);
    // printf("Time: %f seconds\n", elapsed2);
}